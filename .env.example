# Copy to .env and fill in. .env is gitignored; .env.example is committed.

# Supabase Postgres connection string (BRIEF §5: Supabase, eu-central-1).
# Use the transaction-pooler (pgbouncer, port 6543) URL for the app, and
# the direct (port 5432) URL for drizzle-kit push/migrate/studio.
# Example: postgresql://postgres:[PASSWORD]@db.[REF].supabase.co:5432/postgres
DATABASE_URL=
